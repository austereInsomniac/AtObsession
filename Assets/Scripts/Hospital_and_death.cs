using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hospital_and_death : MonoBehaviour
{
    private Vector3 storedLocation = new Vector3 (0, 0, 0);
    private Vector3 cameraLocation = new Vector3(0, 0, 0);
    private int count = 0;

    // Start is called before the first frame update
    void Start()
    {
        //if (count == 0){
        //  if (wellness == 0){
        //      
        //      count++;
        //  }
        //
        //}
        //
        //if (count == 1){
        //  
        //}

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
